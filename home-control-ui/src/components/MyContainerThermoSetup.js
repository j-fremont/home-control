import React from 'react';

import Container from '@mui/material/Container';

const MyContainerThermoSetup = ({ container }) => {

	return (
		<React.Fragment>
			{container}
		</React.Fragment>
	)
}

export default MyContainerThermoSetup
